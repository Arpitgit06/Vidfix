# AV-SynthRestore 3D – Python Backend Dependencies
# Install with: pip install -r requirements.txt
#
# GPU-accelerated inference (CUDA):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Real-ESRGAN (optional but recommended for true 4K upscaling):
#   pip install realesrgan basicsr
#   Download weights: https://github.com/xinntao/Real-ESRGAN/releases
#   Place: ./weights/RealESRGAN_x4plus.pth

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Required for UploadFile/File()

# ── Audio Processing ───────────────────────────────────────────────────────────
librosa==0.10.2
soundfile==0.12.1
scipy>=1.11.0
numpy>=1.24.0,<2.0.0

# ── Video / Image Processing ───────────────────────────────────────────────────
opencv-python-headless==4.9.0.80  # Use -headless to avoid GUI deps in server env
ffmpeg-python==0.2.0               # ffmpeg-python bindings (requires ffmpeg binary on PATH)

# ── Deep Learning (CPU fallback if CUDA not available) ────────────────────────
torch>=2.1.0                       # PyTorch for bicubic GPU upscale + ESRGAN
torchvision>=0.16.0
torchaudio>=2.1.0                  # Required for Audio AI Models

# ── AI Audio Models (Fish Audio & FireRedTTS) ─────────────────────────────────
huggingface_hub>=0.23.0
transformers>=4.40.0


# ── Optional: Real-ESRGAN (4K AI upscaling) ───────────────────────────────────
# Uncomment if you have GPU and want true AI upscaling (not just bicubic):
realesrgan>=0.3.0
basicsr-fixed

# ── Optional: AudioLDM2 (deep generative audio inpainting) ────────────────────
# Requires ~8 GB VRAM. Uncomment to enable:
# audioldm2
# transformers>=4.37.0
# diffusers>=0.25.0

# ── Utilities ─────────────────────────────────────────────────────────────────
aiofiles==23.2.1
httpx==0.27.0                      # Async HTTP client (useful for health checks)
